{
  "name": "github-quickview",
  "version": "0.3.0",
  "private": true,
  "description": "GitHub Quickview keeps long pull requests navigable.",
  "scripts": {
    "check": "node --check src/core.js && node --check src/content.js && node --check test/fixture-server.mjs && node -e \"JSON.parse(require('node:fs').readFileSync('manifest.json', 'utf8'))\"",
    "fixture": "node test/fixture-server.mjs",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
